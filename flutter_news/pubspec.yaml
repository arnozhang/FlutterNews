name: flutter_news
description: News application build with Flutter.

dependencies:
  flutter:
    sdk: flutter

  flutter_webview_plugin: ^0.3.3
  cached_network_image: ^0.7.0
  cupertino_icons: ^0.1.2


dev_dependencies:
  flutter_test:
    sdk: flutter



# The following section is specific to Flutter.
flutter:
  uses-material-design: true

  assets:
    - assets/images/naga.png

